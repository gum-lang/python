[project]
name = "gum"
version = "0.1.0"
description = "a gum parser for python"
requires-python = ">=3.10"

[project.optional-dependencies]
dev = ["pytest"]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
include = ["gum*"]

[tool.pytest.ini_options]
testpaths = ["tests"]
